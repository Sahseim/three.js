export default /* glsl */`
#ifdef USE_TEXTURE_SWITCH
	/*
	vec4 texelColorMul = texture2D( textureSwitchMap, vUv / textureSwitchScale );

	texelColorMul = mapTexelToLinear( texelColorMul );
	diffuseColor *= texelColorMul;*/

#endif
`;